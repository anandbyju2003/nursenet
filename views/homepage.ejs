<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <div>
        <h1>Records</h1>
        <% data.forEach(record => { %>
            <div class="record">
                <!-- Build your HTML structure for each record here -->
                <h2><%= record.name %></h2>
            </div>
        <% }) %>
    </div>
</body>
</html>